[package]
name = "mini-kvstore-v2"
version = "0.1.0"
edition = "2021"
authors = ["whispem"]
description = "mini-kvstore-v2 — segmented append-only KV store (learning project)"
license = "MIT"

[lib]
name = "mini_kvstore_v2"
path = "src/lib.rs"

[[bin]]
name = "mini-kvstore-v2"
path = "src/main.rs"

[[bench]]
name = "kvstore_bench"
harness = false

[dependencies]
anyhow = "1.0"
crc32fast = "1.3"

[dev-dependencies]
criterion = "0.5"
```

4. Commit : `"Add benchmark configuration and criterion dependency"`

---

## ÉTAPE 4 : Mettre à jour `.gitignore`

**Sur GitHub :**
1. Va sur le fichier `.gitignore`
2. Clique sur ✏️ (Edit)
3. **REMPLACE TOUT** par ce contenu :
```
# Generated by Cargo
# will have compiled files and executables
debug/
target/

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Generated by cargo mutants
# Contains mutation testing data
**/mutants.out*/

# RustRover
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# Store data directories
data/
tests_data/
bench_data/
example_data/
compaction_example/
persistence_example/
large_dataset_example/

# OS files
.DS_Store
Thumbs.db
