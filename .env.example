# Example environment configuration for mini-kvstore-v2
# Copy this file to .env and modify as needed

# Server Configuration
PORT=8000
VOLUME_ID=vol-1
DATA_DIR=./data

# Logging (future)
# RUST_LOG=info
# RUST_BACKTRACE=1

# Performance Tuning (future)
# MAX_SEGMENT_SIZE=33554432  # 32 MB
# COMPACTION_THRESHOLD=50    # Number of segments before auto-compaction
# INDEX_CACHE_SIZE=10000     # Number of keys to cache

# Security (future)
# API_KEY=your-secret-key-here
# TLS_CERT=/path/to/cert.pem
# TLS_KEY=/path/to/key.pem

# Monitoring (future)
# METRICS_PORT=9090
# HEALTH_CHECK_INTERVAL=30

# Development
# DEV_MODE=true
# HOT_RELOAD=true
